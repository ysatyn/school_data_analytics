# Импорт библиотеки pandas для работы с табличными данными
import pandas as pd  

# Устанавливаем опцию, чтобы Pandas явно предупреждал о проблемах с неявным преобразованием типов
pd.set_option('future.no_silent_downcasting', True)

# Загружаем данные из CSV-файла с ответами на опрос в DataFrame (таблицу данных)
df = pd.read_csv("Тест 10 классов - Ответы на форму.csv")

# Список всех учебных предметов, представленных в опросе
subjects = [
    "Алгебра", "Геометрия", "Вероятность и статистика", "Русский язык", "Литература", "История",
    "Английский язык", "Второй иностранный язык", "География", "Физика", "Химия", "Биология",
    "ОБЗР", "Информатика", "Физическая культура", "Обществознание"
]

# Словарь для преобразования текстовых ответов об успеваемости в числовые значения (1-5)
grade_map = {
    "Я всегда учусь плохо": 1, "Я чаще всего учусь плохо": 2,
    "Я иногда учусь хорошо, иногда плохо": 3, "Я чаще всего учусь хорошо": 4,
    "Я всегда учусь хорошо": 5
}

# Словарь для преобразования текстовых ответов об интересе к предмету в числовые значения (1-5)
interest_map = {
    "Мне всегда скучно на уроке": 1, "Мне чаще всего бывает скучно на уроке": 2,
    "Мне иногда скучно, иногда интересно на уроке": 3, "Мне часто бывает интересно на уроке": 4,
    "Мне всегда интересно на уроке": 5
}

# Словарь для преобразования текстовых ответов о комфорте на уроке в числовые значения (1-5)
comfort_map = {
    "Я всегда испытываю дискомфорт на уроке (тревогу, страх, грусть)": 1,
    "Я часто испытываю дискомфорт на уроке (тревогу, страх, грусть)": 2,
    "Мне иногда эмоционально комфортно на уроке, иногда я испытываю дискомфорт (тревогу, страх, грусть)": 3,
    "Мне чаще всего бывает эмоционально комфортно на уроке": 4,
    "Мне всегда эмоционально комфортно на уроке": 5
}

# Функция для расчета и форматирования распределения ответов в процентном соотношении
def format_distribution(df_class, column, options):
    total = len(df_class)  # Общее количество ответов в классе
    counts = df_class[column].value_counts()  # Подсчет количества каждого варианта ответа
    # Формирование списка строк с процентным соотношением для каждого варианта ответа
    return [f"\"{opt}\" — {round(counts.get(opt, 0)/total*100)}%" for opt in options]

# Функция для вывода статистики по конкретному предмету в конкретном классе
def subject_in_class(cls, subject):
    # Фильтрация данных по указанному классу (без учета регистра)
    df_cls = df[df["Мой класс"].str.lower() == cls.lower()]
    if df_cls.empty:  # Проверка наличия данных для этого класса
        print("Нет данных по классу.")
        return

    print(f"\n📘 {subject.upper()} в классе {cls}")  # Заголовок вывода

    # Перебор всех трех критериев (успеваемость, интерес, комфорт)
    for name, mapping in [("Как я учусь?", grade_map), ("Интересно ли мне учиться?", interest_map), ("Как я чувствую себя на уроке?", comfort_map)]:
        # Поиск столбца, соответствующего критерию и предмету
        col = next((c for c in df.columns if name in c and subject.lower() in c.lower()), None)
        if col:  # Если столбец найден
            # Вывод распределения ответов
            print(f"\n{name}:\n" + "\n".join(format_distribution(df_cls, col, mapping.keys())))

# Функция для генерации отчета по всем предметам в указанном классе
def class_report(cls):
    for subj in subjects:  # Перебор всех предметов
        subject_in_class(cls, subj)  # Вывод статистики по каждому предмету

# Функция для генерации отчета по указанному предмету во всех классах
def subject_report(subj):
    for cls in df["Мой класс"].unique():  # Перебор всех уникальных классов
        subject_in_class(cls, subj)  # Вывод статистики для каждого класса

# Основная функция для генерации сводного отчета по всем классам и предметам
def main_report():
    print("===== Общий отчёт по всем классам =====")
    for cls in df["Мой класс"].unique():  # Перебор всех уникальных классов
        df_cls = df[df["Мой класс"] == cls]  # Фильтрация данных по классу
        print(f"\n📚 Класс {cls}")  # Заголовок класса

        # Перебор всех трех критериев оценки
        for name, mapping, title in [
            ("Как я учусь?", grade_map, "Успеваемости"),
            ("Интересно ли мне учиться?", interest_map, "Интересу"),
            ("Как я чувствую себя на уроке?", comfort_map, "Комфорту")
        ]:
            # Поиск всех столбцов, соответствующих критерию
            cols = [c for c in df.columns if name in c]
            # Преобразование текстовых ответов в числовые и расчет среднего значения
            avg = df_cls[cols].replace(mapping).infer_objects(copy=False).apply(pd.to_numeric, errors='coerce').mean().sort_values(ascending=False)

            # Вывод ТОП-3 предметов по текущему критерию
            print(f"\n⭐ ТОП-3 лучших по {title}:\n" + "\n".join(avg.head(3).index.str.extract(r"\[(.*?)\]")[0]))
            # Вывод АНТИ-ТОП-3 предметов по текущему критерию
            print(f"🔻 ТОП-3 худших по {title}:\n" + "\n".join(avg.tail(3).index.str.extract(r"\[(.*?)\]")[0]))

# Запуск основного отчета
main_report()

# Цикл для интерактивного взаимодействия с пользователем
while True:
    # Запрос ввода от пользователя (класс, предмет или оба)
    inp = input("\nВведите класс, предмет или оба. Enter — выход: ").strip().lower()
    if not inp:  # Если ввод пустой - выход из цикла
        break  

    # Поиск класса во вводе пользователя
    found_class = next((c for c in df["Мой класс"].unique() if c.lower() in inp), None)
    # Поиск предмета во вводе пользователя
    found_subject = next((s for s in subjects if s.lower() in inp), None)

    # Определение типа запроса и вызов соответствующей функции
    if found_class and found_subject:  # Если указаны и класс и предмет
        subject_in_class(found_class, found_subject)
    elif found_class:  # Если указан только класс
        class_report(found_class)
    elif found_subject:  # Если указан только предмет
        subject_report(found_subject)
    else:  # Если ничего не найдено
        print("Не найдено.")