import pandas as pd  # Импорт библиотеки pandas для работы с таблицами (DataFrame)

# Устанавливаем опцию, чтобы в будущем Pandas явно указывал на проблемы с преобразованием типов
pd.set_option('future.no_silent_downcasting', True)

# Загружаем CSV-файл с результатами опроса в таблицу DataFrame
df = pd.read_csv("Тест 10 классов - Ответы на форму.csv")

# Список всех предметов, которые есть в опросе
subjects = [
    "Алгебра", "Геометрия", "Вероятность и статистика", "Русский язык", "Литература", "История",
    "Английский язык", "Второй иностранный язык", "География", "Физика", "Химия", "Биология",
    "ОБЗР", "Информатика", "Физическая культура", "Обществознание"
]

# Сопоставление вариантов ответа про успеваемость с числовыми значениями от 1 до 5
grade_map = {
    "Я всегда учусь плохо": 1, "Я чаще всего учусь плохо": 2,
    "Я иногда учусь хорошо, иногда плохо": 3, "Я чаще всего учусь хорошо": 4,
    "Я всегда учусь хорошо": 5
}

# Сопоставление вариантов ответа про интерес к предмету с числовыми значениями от 1 до 5
interest_map = {
    "Мне всегда скучно на уроке": 1, "Мне чаще всего бывает скучно на уроке": 2,
    "Мне иногда скучно, иногда интересно на уроке": 3, "Мне часто бывает интересно на уроке": 4,
    "Мне всегда интересно на уроке": 5
}

# Сопоставление вариантов ответа про эмоциональный комфорт на уроке с числами от 1 до 5
comfort_map = {
    "Я всегда испытываю дискомфорт на уроке (тревогу, страх, грусть)": 1,
    "Я часто испытываю дискомфорт на уроке (тревогу, страх, грусть)": 2,
    "Мне иногда эмоционально комфортно на уроке, иногда я испытываю дискомфорт (тревогу, страх, грусть)": 3,
    "Мне чаще всего бывает эмоционально комфортно на уроке": 4,
    "Мне всегда эмоционально комфортно на уроке": 5
}

# Функция для форматирования статистики: сколько процентов учеников выбрало каждый вариант
def format_distribution(df_class, column, options):
    total = len(df_class)  # Общее количество ответов
    counts = df_class[column].value_counts()  # Подсчитываем количество ответов каждого типа
    # Возвращаем список строк с процентами для каждого варианта ответа
    return [f"\"{opt}\" — {round(counts.get(opt, 0)/total*100)}%" for opt in options]

# Функция показывает информацию по одному предмету в одном классе
def subject_in_class(cls, subject):
    # Фильтруем таблицу по нужному классу (без учёта регистра)
    df_cls = df[df["Мой класс"].str.lower() == cls.lower()]
    if df_cls.empty:
        print("Нет данных по классу.")
        return

    print(f"\n📘 {subject.upper()} в классе {cls}")

    # Для каждого вопроса (успеваемость, интерес, комфорт) ищем соответствующую колонку и выводим статистику
    for name, mapping in [("Как я учусь?", grade_map), ("Интересно ли мне учиться?", interest_map), ("Как я чувствую себя на уроке?", comfort_map)]:
        # Ищем колонку, которая содержит и нужный вопрос, и предмет
        col = next((c for c in df.columns if name in c and subject.lower() in c.lower()), None)
        if col:
            # Выводим распределение ответов
            print(f"\n{name}:\n" + "\n".join(format_distribution(df_cls, col, mapping.keys())))

# Функция показывает отчёт по всем предметам в одном классе
def class_report(cls):
    for subj in subjects:
        subject_in_class(cls, subj)

# Функция показывает отчёт по одному предмету во всех классах
def subject_report(subj):
    for cls in df["Мой класс"].unique():
        subject_in_class(cls, subj)

# Главный отчёт: показывает Топ-3 и Анти-топ-3 предметов по всем критериям (успеваемость, интерес, комфорт)
def main_report():
    print("===== Общий отчёт по всем классам =====")
    for cls in df["Мой класс"].unique():  # Для каждого уникального класса
        df_cls = df[df["Мой класс"] == cls]  # Фильтруем данные по этому классу
        print(f"\n📚 Класс {cls}")

        # Для каждого показателя (успеваемость, интерес, комфорт)
        for name, mapping, title in [
            ("Как я учусь?", grade_map, "Успеваемости"),
            ("Интересно ли мне учиться?", interest_map, "Интересу"),
            ("Как я чувствую себя на уроке?", comfort_map, "Комфорту")
        ]:
            # Находим все колонки, относящиеся к нужному вопросу
            cols = [c for c in df.columns if name in c]
            # Преобразуем ответы в числа и считаем среднее по каждому предмету
            avg = df_cls[cols].replace(mapping).infer_objects(copy=False).apply(pd.to_numeric, errors='coerce').mean().sort_values(ascending=False)

            # Показываем 3 предмета с самым высоким и самым низким средним баллом
            print(f"\n⭐ ТОП-3 по {title}:\n" + "\n".join(avg.head(3).index.str.extract(r"\[(.*?)\]")[0]))
            print(f"🔻 АНТИ-ТОП-3 по {title}:\n" + "\n".join(avg.tail(3).index.str.extract(r"\[(.*?)\]")[0]))

# Запускаем основной отчёт по всем классам
main_report()

# Цикл пользовательского ввода для генерации отчётов по конкретному запросу
while True:
    # Получаем ввод от пользователя
    inp = input("\nВведите класс, предмет или оба. Enter — выход: ").strip().lower()
    if not inp:
        break  # Выход из цикла по пустому вводу

    # Пытаемся найти класс и предмет в пользовательском вводе
    found_class = next((c for c in df["Мой класс"].unique() if c.lower() in inp), None)
    found_subject = next((s for s in subjects if s.lower() in inp), None)

    # В зависимости от найденного — вызываем соответствующую функцию
    if found_class and found_subject:
        subject_in_class(found_class, found_subject)
    elif found_class:
        class_report(found_class)
    elif found_subject:
        subject_report(found_subject)
    else:
        print("Не найдено.")  # Если ничего не найдено — выводим сообщение
